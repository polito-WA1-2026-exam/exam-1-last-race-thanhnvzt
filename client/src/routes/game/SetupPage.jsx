export function SetupPage() {
  return (
    <section className="page-panel">
      <h1>Setup</h1>
      <p>
        Phase 0 placeholder. The full network map and start-game action will be
        implemented after the backend seed and network API exist.
      </p>
    </section>
  );
}
